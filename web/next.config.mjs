/** @type {import('next').NextConfig} */
const nextConfig = {
    // For local development with API routes, keep these commented
    // For GitHub Pages deployment, uncomment the lines below:
    // output: 'export',
    // basePath: '/7k-card-matching-solver',
    // assetPrefix: '/7k-card-matching-solver',
    
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
